apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedagogy-engine
  namespace: ai-platform
  labels:
    app: pedagogy-engine
    tier: educational
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pedagogy-engine
  template:
    metadata:
      labels:
        app: pedagogy-engine
        tier: educational
    spec:
      containers:
      - name: pedagogy-engine
        image: pedagogy-engine:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8014
          name: http
        - containerPort: 50063
          name: grpc
        env:
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: url
        - name: ENABLE_GRPC_SERVER
          value: "true"
        - name: ENABLE_RABBITMQ_CONSUMER
          value: "true"
        - name: GRPC_PORT
          value: "50063"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8014
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8014
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: pedagogy-engine
  namespace: ai-platform
  labels:
    app: pedagogy-engine
spec:
  type: ClusterIP
  ports:
  - port: 8014
    targetPort: 8014
    name: http
  - port: 50063
    targetPort: 50063
    name: grpc
  selector:
    app: pedagogy-engine
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pedagogy-engine-hpa
  namespace: ai-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pedagogy-engine
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
