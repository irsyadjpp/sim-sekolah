apiVersion: apps/v1
kind: Deployment
metadata:
  name: curriculum-engine
  namespace: ai-platform
  labels:
    app: curriculum-engine
    tier: educational
spec:
  replicas: 2
  selector:
    matchLabels:
      app: curriculum-engine
  template:
    metadata:
      labels:
        app: curriculum-engine
        tier: educational
    spec:
      containers:
      - name: curriculum-engine
        image: curriculum-engine:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8013
          name: http
        - containerPort: 50062
          name: grpc
        env:
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: url
        - name: ENABLE_GRPC_SERVER
          value: "true"
        - name: ENABLE_RABBITMQ_CONSUMER
          value: "true"
        - name: GRPC_PORT
          value: "50062"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8013
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8013
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: curriculum-engine
  namespace: ai-platform
  labels:
    app: curriculum-engine
spec:
  type: ClusterIP
  ports:
  - port: 8013
    targetPort: 8013
    name: http
  - port: 50062
    targetPort: 50062
    name: grpc
  selector:
    app: curriculum-engine
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: curriculum-engine-hpa
  namespace: ai-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: curriculum-engine
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
