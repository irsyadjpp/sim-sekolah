apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-graph-engine
  namespace: ai-platform
  labels:
    app: learning-graph-engine
    tier: educational
spec:
  replicas: 2
  selector:
    matchLabels:
      app: learning-graph-engine
  template:
    metadata:
      labels:
        app: learning-graph-engine
        tier: educational
    spec:
      containers:
      - name: learning-graph-engine
        image: learning-graph-engine:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8017
          name: http
        - containerPort: 50066
          name: grpc
        env:
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: url
        - name: ENABLE_GRPC_SERVER
          value: "true"
        - name: ENABLE_RABBITMQ_CONSUMER
          value: "true"
        - name: GRPC_PORT
          value: "50066"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8017
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8017
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: learning-graph-engine
  namespace: ai-platform
  labels:
    app: learning-graph-engine
spec:
  type: ClusterIP
  ports:
  - port: 8017
    targetPort: 8017
    name: http
  - port: 50066
    targetPort: 50066
    name: grpc
  selector:
    app: learning-graph-engine
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: learning-graph-engine-hpa
  namespace: ai-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: learning-graph-engine
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
